# v0.7 验证记录

日期：2026-09-23。以下由实现者在本机执行；不是独立玩家、真实手机耳机或鸟类专家验收。

- `npm test`：27 项通过。题库为 110 个不同学名、11 个有上限的候选池；新增 e21–e110 的 90 道题均有本地图片。71 道含可归属的 Commons / Xeno-canto 真实参考照片和录音；19 道图片带 `generated: true` 元数据、界面标为 AI 教学插图，并仅使用资料化现场听记。
- `python3 build.py`：生成 `芦汀观测站.html`，大小 55,539,224 字节。测试逐项确认所有图片和录音以 data URI 内嵌；19 张 AI 教学插图均由 FFmpeg 解码通过。
- 数据边界：AI 图只辅助模拟“看见了哪些外形片段”，不会计作真实照片、真实观察或独立定种依据；19 道题没有播放或合成鸟鸣。
- 浏览器层面的 v0.7 回归尚未在本执行环境完成：本地 HTTP 监听与 Playwright 的 macOS 进程注册均被沙箱拒绝。因此没有把旧版浏览器测试结果冒充为本版结果；发布后仍应在真实 Chrome/Safari 和耳机上复查。

边界：题目情境中的地点、时刻、天气、数量、观察顺序和预测结果仍为虚构。来源支持编写与区域约束，不等于鸟类专家逐题审定或真实野外定种保证。

---

# v0.6 验证记录

日期：2026-09-22。以下由实现者在本机执行；不是独立玩家、真实手机耳机或鸟类专家验收。

- `npm test`：27 项通过。检查 100 个不同学名、10 个有上限的候选池（9、20、以及八组各 10）、每题完整上下文、答案属于本组候选、所有新增题带北京出现状态和两条地区资料来源。还检查 80 个新增题没有照片或录音且使用 `field-note`，原有媒体仍有文件、署名、许可与离线嵌入。
- `tests/encounter-browser.cjs`：Chromium 与 WebKit 都通过。每个训练组实际进入一题，完成举镜、真实录音或资料化现场听记、错误与正确位置预测、候选判断和复盘；同时检查 100 题数据、10 个组、天空观察位、深链接和离线文件。真实录音由播放进度触发证据；资料化听记只触发其自身标记，复盘明确未播放实际录音。
- 布局：1440px 宽度实际为 5 列训练组；390px 宽度实际为 2 列且页面没有横向溢出。截图见本地 `evidence/v6-layout-desktop.png`、`v6-layout-mobile.png`、`v6-entry.png` 与 `v6-mobile.png`。
- 回归：`tests/challenge-browser.cjs`、`tests/advanced-browser.cjs`、`tests/browser.cjs` 在 Chromium 均通过，覆盖基础猜鸟、照片进阶与原湿地模拟。
- 离线：`python3 build.py` 生成 `芦汀观测站.html`，大小 23,794,621 字节；逻辑测试逐项确认已有照片与录音作为 data URI 内嵌。离线浏览器验收实际播放一段旧参考录音，并读取一段新增资料化现场听记。

本地证据：`evidence/v6-chromium.json`、`v6-webkit.json`、`v6-entry.png`、`v6-mobile.png`、`v6-layout-desktop.png`、`v6-layout-mobile.png`。测试记录与截图不提交公开仓库。

边界：新增 80 题的声音是资料化文字线索，不能代表可播放录音、声纹或现场采样；也没有把文字伪装成实际听到的声音。Wild Beijing 和北京市名录只作为区域出现约束，并未逐条验证 100 种的当前分类、频率或精确季节。每个地点、时刻、天气、数量、观察顺序和预测结果都是虚构关卡设定。尚未做专家逐题审定、真实手机 Safari／耳机听感、朋友试玩难度与学习效果验证。

---

# v0.5 验证记录

日期：2026-09-22。实现者执行；不是独立玩家或鸟类专家验收。

- `npm test`：29项通过。20个完整遭遇、9/20候选池、唯一学名、别名不重复计数、证据不足关保留候选、ND素材原件校验与全部离线素材嵌入均有覆盖。
- `tests/encounter-browser.cjs`：Chromium和WebKit通过全部20关；播放进度实际推进，FFT有响应。14个新增遭遇不请求照片，镜中笔记明确标为虚构。
- 反例：e11取得全部可用线索后只选作者设定物种，仍显示无法排除相近候选；保留中华／北短翅蝗莺才符合该题证据。错误等待位置不产生补充目击；音频失败不记为已听到；文字辅助不冒充听音。
- 交互：切换9种柳莺专练／20种混合进阶，候选与关卡同步；“下一次遭遇”不越出当前组。两种浏览器均检验390px下混合组实际作答及复盘，无页面横向溢出。检查过桌面及手机截图。
- 存档：20关各两次猜测快照、刷新、历史、导出通过。另以旧v4记录预置存储，验证新版本可继续读取；沿用原键，没有清空旧记录。
- 离线：23,743,976字节单文件包含全部媒体；两浏览器均实际播放旧录音及新增冠纹柳莺原文件，加载原照片。Chromium先断网打开；WebKit打开本地文件后断网。
- 录音：14份新增MP3全部经FFmpeg解码。冠纹柳莺84.65秒与水鹨2.04秒保留原件，SHA-256与下载文件相同；其他12份允许改编，最长24秒，许可和作者逐项写入ASSETS与游戏中。
- 旧玩法：基础6题及照片进阶9题在Chromium回归通过；原生态逻辑13项包含在29项单元检查中。

本地证据：`evidence/v5-chromium.json`、`v5-webkit.json`、`v5-audio-decode.json`、`v5-entry.png`、`v5-mobile.png`、`v5-mixed-mobile.png`。研究原网页和测试档案不提交公开仓库。

边界：20种是本项目有来源的练习集合，非北京完整或全部常见鸟名录；田鹨按学名合并理氏鹨。地理、季节与鉴别资料有出处，具体时空、天气、目击经过和预测分支为虚构。录音来自不同种群与地点，页面标注了差异。浏览器可播放不等于专家听音鉴定；未做真实手机耳机听感、专家逐题审定、玩家难度或朋友满意度验证。

---

# v0.4 验证记录

日期：2026-09-22。以下为实现者执行的自动化与截图检查，不是独立玩家或鸟类专家验收。

- `npm test`：27 项通过。新增六项检查完整场景与媒体来源、重复证据去重、猜中但缺证据不通过、多候选/错误/不确定分开、历史假设快照、地理与声音边界及单文件素材完整性。
- `tests/encounter-browser.cjs`：Chromium、WebKit 通过全部六次遭遇。实际听音时间推进、音频 FFT 非零、初始不展示照片、图片单独不足以结束、先记猜测后修改、错误预测不产生补充目击、正确预测后六关均可完成、结束后资料/虚构分开。
- 反例：正确鸟名但没有补充目击时结果为证据不足；录音请求失败不写入已听到证据；文字辅助记录为文字而非实际听音。
- 持久化与导出：六关记录、各两次中途假设、刷新保留、历史查看和 JSON 导出通过。新遭遇使用独立键，不覆盖旧练习记录。
- 音频切换：离开遭遇页暂停新录音；离开旧练习页暂停旧录音。默认无自动播放。图片与声音深链接及离线版通过。
- 移动端：390px 宽度无页面横向溢出，可以从收集线索到提交复盘；案例条在窄屏内部横向滚动，首末关可操作。看过桌面入口和手机截图。
- `tests/challenge-browser.cjs`、`tests/advanced-browser.cjs`：Chromium 基础六题与九张照片题回归通过。回归测试改为明确打开旧模式、在旧面板定位输入；初次失败来自新增隐藏面板也有同名候选，修正测试定位后通过，未删除原验收项。
- 六个新 MP3 都经 FFmpeg 完整解码；长度约 5.59、18.05、12.98、18.05、55.14、18.05 秒。极北片段保留完整叫声/鸣唱组合。音量统一、短淡入淡出；无合成鸟鸣。
- 单文件约 14.4 MB。Chromium 先断网再打开；WebKit 先打开本地文件再断网，之后播放内嵌声音、加载内嵌照片通过。

本地证据：`evidence/v4-chromium.json`、`v4-webkit.json`、`v4-audio-decode.json`、`v4-entry.png`、`v4-review.png`、`v4-mobile.png`。测试导出与研究缓存不提交公开仓库。

局限：真实手机/耳机听感尚未实机验证；声音播放与 FFT 通过不代表独立生物声学鉴定。鸟类资料与来源标签支持题目编写，但精确天气、数量、时刻、移动与目击顺序是虚构，预测为固定情境分支。没有进行鸟类专家逐题审定，也未验证实际难度、学习效果或朋友满意度。

---

# v0.3 验证记录

日期：2026-09-22。测试由实现者执行；不等于朋友满意度、真实手机或专家鉴别验收。

- `npm test`：21 项通过，包括既有 13 项生态推断、4 项基础题与 4 项进阶数据/复盘/素材/离线检查。进阶匹配素材标签时不产生正确率；`correct` 为 `null`。
- `tests/advanced-browser.cjs`：Chromium、WebKit 均通过。实际加载全部 9 张新照片，检查无人工模糊滤镜、提交前无对照图、最多三候选、暂不定种互斥、候选持久化、来源标签复盘、高把握复核、九题循环、切题清空复盘、基础音频切换停止、深链接及 390px 无横向溢出。
- 进阶离线版打开、照片显示、六张复盘图片完整加载通过。Chromium 先断网再打开文件；WebKit 先打开本地文件再切断网络。单文件约 9.3 MB。
- `tests/challenge-browser.cjs`：Chromium 基础六题回归通过，含五张旧照片、三段实录、答错与开放题、历史导出、原自由观测启动、录音失败不伪造声音证据、移动宽度与离线音频。
- 查看了素材候选联系表、桌面照片题与手机截图；9 张源图均保持平台缩略图原有构图，画面没有生成、人工模糊或特征补全。
- 本地证据：`evidence/v3-chromium.json`、`v3-webkit.json`、`v3-desktop.png`、`v3-mobile.png`、`v3-review.png`。测试浏览器与用户日常浏览器记录隔离，证据目录不公开提交。

尚未验证：朋友实际游玩难度反馈、真实手机 Safari 与耳机、鸟类专家逐题审定。六候选不是完整北京名录；9 张照片没有配套现场录音；跨地区、亚种和当前分类的适用边界在素材说明中保留。

---

# v0.2 现场猜鸟验收（2026-09-22）

这次验证针对默认猜鸟玩法，均由实现者执行，不代替独立玩家试玩。

- `node --test tests/*.test.cjs`：17 项通过，包括六题结构、开放案例不伪造答案、把握程度反馈、素材署名，以及原有 13 项模拟测试。
- Chromium：完整完成六题；五张真实参考图片加载，三段 MP3 实际播放且播放时间递增。无录音的案例明确提示缺失。
- WebKit 26.5：完整答题、媒体播放、记录和手机宽度交互通过；本地单文件先载入再断网，嵌入图片与录音仍可使用。WebKit 自动化的“断网后再导航 file://”返回内部错误，因此与 Chromium 的断网导航路径分开报告。
- 检查答题前隐藏答案和候选概率；提交后显示关键特征与四项易混对照。正确、错误高把握、暂不定种分别验证。
- 检查重复尝试、刷新恢复、历史查看、JSON 导出；不会将私人笔记上传到服务器。
- 模式切换停止正在播放的音频，自由观测的声音也会在离开后暂停；旧模式的完整浏览器回归通过。
- 390 px 手机宽度完成答题，无横向溢出；图片保持完整，不裁掉尾羽和嘴等辨认部位。
- 模拟录音请求失败：不将其计为已听到的声音证据，并给出可读提示。
- 所有三段音频经过 FFmpeg 完整解码检查；本地单文件约 5 MB，内嵌图片、录音和脚本。Chromium 断网打开并实际播放嵌入音频通过。

测试结果及截图在本地 `evidence/v2-*`；测试数据不提交到公开仓库。`tests/challenge-browser.cjs` 支持 `TEST_WEBKIT=1` 和 `BASE_URL` 用于额外引擎与已部署页面检查。

声音证据证明浏览器成功播放媒体，不证明真实手机扬声器或耳机听感。动作是程序化示意，照片和录音是带来源的参考素材，题目情境不是同一现场实录。北京案例保留原记录的不确定性，没有强行认定鸟种。

---

# v0.1 验证记录

日期：2026-09-20。以下为实现者执行的自动测试与截图检查，不是独立玩家评价。

## 已验证

`node --test tests/engine.test.cjs`：13 项通过。

- 概率归一化与固定数值样例；未知分支保持非零。
- 同一类型的重复证据不重复计权；推断不读取物种真值字段。
- 类鸟机械音可产生错误第一候选，冷目标与目视证据可推翻它。
- 温暖的松鼠不会被目视记录成鸟；浓密遮挡同时限制光学与热像。
- 偏离方向、太远、植被遮挡、飞行模糊可阻止确认。
- 六种鸟在满足距离与清晰度条件时都能确认。此项使用受控状态，不意味着已在自然游玩中逐一找到六种。
- 昼夜活动偏好、脚步干扰及恢复、固定种子的重放、种群不凭空增加、坐标边界、跨日时钟。
- 静候按钮的模拟时间正好前进五分钟；薄雾缩小光学有效距离。

`tests/browser.cjs`：在 Chromium 中通过完整交互流程。

- 从自然生成的第一条声音开始，完成翠鸟的声纹、环境、热像、预测、光学确认、笔记保存。
- 自然生成塑料袋假信号，经采样、热像与光学确认后，保存为非鸟类记录。
- 浏览器 AudioContext 运行，实时 FFT 数据非零。此证据证明音频图正常产生信号，不证明实际扬声器或耳机的听感。
- 档案刷新后保留；JSON 导出含证据与预测结果。
- 暂停冻结时间；闭眼模式、天气切换、观察点切换、时间轴与等待可操作。
- 1440 px 桌面视图与 390 px 窄屏截图；无横向溢出，拖动转向有效。
- 六种识别手册卡片正常显示。
- 切断浏览器网络后，单文件 HTML 能直接打开、启动音频并生成信号。
- 无页面 JavaScript 异常，页面资源请求限于本地预览源。

结果文件：`evidence/browser-results.json`。截图覆盖入口、野外视野、热像、确认、假信号、档案与手机宽度页面。

## 尚未验证

- 真实手机 Safari、实体 AirPods、扬声器左右声道和实际听感。
- 长时间游玩的难度、乐趣及新玩家能否独立理解教程。
- 真实生态与真实声纹准确度。本项目没有此类校准数据，也不作此类保证。

## 可复现性

默认世界使用固定种子 `20260920`，便于复现第一次观察；帧推进和用户操作时机会改变后续随机调用顺序与事件时刻。测试只通过正常界面操作完成浏览器观察流程，没有注入物种位置或强制生成鸟类。逻辑层的受控条件测试与自然交互测试分别报告。
